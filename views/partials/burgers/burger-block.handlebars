<div class="row d-flex flex-wrap">
<li class="text-center col border border-success">
	<p>{{burger_name}}</p>
</li>
	{{#if eaten}}
	<button class="delete col: width of 15% mb-5" data-id="{{id}}">Delete From Table!
	</button>

	{{else}}
	<button class="devour col: width of 15% mb-5" data-id="{{id}}">Devour It!
	</button>
	{{/if}}
</div>
