<div>
<img class="myBurger rounded mx-auto d-block" src="./assets/images/burger2.gif">
</div>

<h1 class="text-center mt-5 text-danger text-decoration-underline">Eat-Da-Burger</h1>

<div class="row">
 <div class="col-md-6 d-flex justify-content-center">
<ol>
  <h3 class="text-center mt-3 mb-3">Burgers To Eat!</h3>
  <ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block eaten=false}}
    {{/unless}}
  {{/each}}
  </ul>
</ol>
</div> 
<div class="col-md-6 d-flex justify-content-center">
<ol>
  <h3 class="text-center mt-3 mb-3">You Ate Them All!</h3>
  <ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block eaten=true}}
    {{/if}}
  {{/each}}
  </ul>
</ol>
</div>
</div>
<form class="create-form mt-5">

  <div class="form-group">
    <label for="burg"></label>
    <textarea name="name" id="burg" cols="50" rows="3" placeholder="Type your burger's name"
    ></textarea>
  </div>

  <button class="text-white" type="submit">Add Burger</button>
  <h5 class="mt-3">Enter a burger you would like to eat and click Add Burger button.</h5>
</form>
